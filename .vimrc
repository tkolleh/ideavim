" automatically switch to case sensitive search if using capital letters else ignore case
:set ignorecase
:set smartcase

" mapp jj to <ESC> when in insert mode
imap jj <ESC>

" Set clipboard to system clipboard
set clipboard+=unnamed
